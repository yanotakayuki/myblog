<div class="main-content">
  <div class="row contributor"><p><%= @nickname %>さんの投稿一覧</p>
    <div class="blog-list">
      <% @blogs.each do |blog| %>
        <div class="card darken-1 ">
          <div class="card-content black-text">
            <p><%= blog.text %></p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
